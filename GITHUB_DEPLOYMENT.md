# 🚀 Инструкция по загрузке проекта на GitHub

## ✅ Подготовка завершена

Все необходимые файлы созданы:
- ✅ `.gitignore` - обновлен (исключает `.env`, `venv/`, `__pycache__/`, и др.)
- ✅ `README.md` - создан с полным описанием проекта
- ✅ Токены безопасно хранятся в `.env` (файл в `.gitignore`)

## 🔒 КРИТИЧНО: Проверка безопасности перед push

### 1. Проверьте, что `.env` в `.gitignore`
```powershell
# Убедитесь, что эта команда НЕ показывает .env файл
git status
```

**Если `.env` отображается в списке** → STOP! Не пушьте! Добавьте `.env` в `.gitignore`.

### 2. Проверьте отсутствие hardcoded токенов в коде

✅ **Проверено автоматически:**
- `config.py` - токены загружаются из переменных окружения ✅
- `ai_service.py` - использует `settings.gemini_api_key` ✅
- Hardcoded токены НЕ найдены ✅

### 3. Убедитесь, что БД не коммитится
```powershell
# Эта команда НЕ должна показывать .db файлы
git status | Select-String ".db"
```

## 📋 Команды для загрузки на GitHub

### Вариант 1: Если git уже инициализирован (ваш случай)

```powershell
# 1. Проверьте текущий статус
git status

# 2. Добавьте все файлы (кроме игнорируемых)
git add .

# 3. Создайте коммит
git commit -m "Initial commit: UQ-bot RAG system with multitenancy

- Telegram bot with aiogram 3.x
- RAG system with Google Gemini 2.5 Flash
- FAISS vector store for knowledge base
- Multi-language support (ru, en, kk, zh)
- Department-based document isolation
- Hierarchical admin panel for KB management
- Voice message support (STT)"

# 4. Привяжите удаленный репозиторий
git remote add origin https://github.com/ArmanAbenov/bot.git

# 5. Отправьте код в GitHub
git push -u origin main
```

### Вариант 2: Если нужно пересоздать git репозиторий

```powershell
# 1. Удалите существующий .git (если нужно начать заново)
Remove-Item -Path .git -Recurse -Force

# 2. Инициализируйте новый репозиторий
git init

# 3. Добавьте все файлы
git add .

# 4. Создайте первый коммит
git commit -m "Initial commit: UQ-bot RAG system"

# 5. Переименуйте ветку в main (если нужно)
git branch -M main

# 6. Привяжите удаленный репозиторий
git remote add origin https://github.com/ArmanAbenov/bot.git

# 7. Отправьте код
git push -u origin main
```

## 🔧 Если возникли проблемы

### Проблема: "remote origin already exists"
```powershell
# Удалите старый remote
git remote remove origin

# Добавьте новый
git remote add origin https://github.com/ArmanAbenov/bot.git
```

### Проблема: "refused to merge unrelated histories"
```powershell
# Если в удаленном репозитории уже есть коммиты (например, README)
git pull origin main --allow-unrelated-histories

# Затем запушьте
git push -u origin main
```

### Проблема: ".env файл попал в коммит"
```powershell
# STOP! Не пушьте! Удалите файл из git (но не с диска)
git rm --cached .env

# Закоммитьте изменение
git commit -m "Remove .env from tracking"

# Теперь можно пушить
git push
```

## 📝 После успешной загрузки

### 1. Проверьте репозиторий на GitHub
Откройте https://github.com/ArmanAbenov/bot и убедитесь:
- ✅ README.md отображается на главной странице
- ✅ `.env` файла НЕТ в репозитории
- ✅ Структура папок корректна

### 2. Добавьте секреты для GitHub Actions (опционально)
Если планируете CI/CD:
1. Перейдите в Settings → Secrets and variables → Actions
2. Добавьте секреты:
   - `TELEGRAM_BOT_TOKEN`
   - `GEMINI_API_KEY`

### 3. Обновите описание репозитория
На странице репозитория добавьте:
- **Description**: "Intelligent RAG-based Telegram bot for employee onboarding with multi-language support"
- **Topics**: `telegram-bot`, `rag`, `gemini`, `aiogram`, `faiss`, `python`

## 🎯 Финальный чеклист

Перед `git push` убедитесь:

- [ ] `.env` в `.gitignore` и не показывается в `git status`
- [ ] `.db` файлы не показываются в `git status`
- [ ] `venv/` и `__pycache__/` игнорируются
- [ ] README.md создан и информативен
- [ ] Нет hardcoded токенов в коде (проверено ✅)
- [ ] Commit message осмысленный
- [ ] Remote URL правильный: `https://github.com/ArmanAbenov/bot.git`

## ✅ Готово к push!

После выполнения всех проверок, запустите финальную команду:

```powershell
git push -u origin main
```

---

**🎉 Поздравляем! Проект на GitHub!**

Теперь коллеги могут:
1. Клонировать репозиторий
2. Создать свой `.env` файл
3. Установить зависимости
4. Запустить бота

**Не забудьте добавить в репозиторий:**
- GitHub Actions для CI/CD (опционально)
- Issues templates
- Pull request template
- Contributing guidelines
