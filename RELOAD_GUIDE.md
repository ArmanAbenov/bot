# 🔄 Гид по перезагрузке RAG индексов

## Быстрая справка

После внесения изменений через админку индексы RAG обновляются **автоматически**. Никаких дополнительных действий не требуется!

## Автоматическое обновление

### ✅ Когда индексы обновляются автоматически:

1. **Добавление файла**
   - Через "📝 Добавить знание" (текст/голос)
   - Через "📥 Добавить файл" (PDF/DOCX/TXT)
   - ⚡ Обновляется индекс конкретного отдела

2. **Удаление файла**
   - Через "📚 Управление базой знаний"
   - ⚡ Обновляется индекс конкретного отдела

## Ручная перезагрузка

### Когда может понадобиться:

- После прямого изменения файлов на сервере (без использования админки)
- После восстановления из бэкапа
- При появлении предупреждения о missing indices

### Как использовать:

```
/reload
```

Отправьте эту команду в бот (доступна только администраторам).

**Результат:**
```
✅ Индексы успешно перезагружены!

📊 Загружено отделов: 5
  • common: 12 чанков
  • sorting: 45 чанков
  • delivery: 38 чанков
  • manager: 23 чанков
  • customer_service: 31 чанков
```

## Диагностика проблем

### Проблема: "Department not found in indices"

**Решение:**
1. Отправьте `/reload` для перезагрузки всех индексов
2. Проверьте что файлы действительно существуют в `data/knowledge/{department}/`
3. Проверьте логи бота на наличие ошибок с префиксом `[RAG]`

### Проблема: Старый контент в ответах

**Решение:**
1. Если файл был удален через админку - индексы должны обновиться автоматически
2. Если файл был изменен напрямую на сервере - отправьте `/reload`

### Проблема: Бот не видит новые файлы

**Причины:**
- Файл сохранен не в ту папку (должен быть в `data/knowledge/{department}/`)
- Неподдерживаемый формат (поддерживаются: .txt, .pdf, .docx, .md, .rst)
- Нет прав на чтение файла

**Решение:**
1. Проверьте путь к файлу
2. Проверьте расширение файла
3. Отправьте `/reload` для принудительной перезагрузки

## Логи

Все операции с индексами логируются с префиксом `[RAG]`:

```bash
# Просмотр логов перезагрузки
grep "\[RAG\]" logs/bot.log | tail -20
```

**Успешная перезагрузка:**
```
[RAG] 🔄 Reloading all indices...
[RAG] Creating department-based vector indices...
[RAG] ✅ Indices reloaded successfully
```

**Проблемы:**
```
[RAG] Error updating vector index: ...
[RAG] WARNING: Department not found in indices
```

## FAQ

**Q: Нужно ли перезапускать бота после изменений в админке?**  
A: Нет! Индексы обновляются автоматически.

**Q: Как часто обновляются индексы?**  
A: Сразу после каждого изменения (добавление/удаление файла).

**Q: Можно ли перезагрузить индексы всем пользователям?**  
A: Нет, команда `/reload` доступна только администраторам.

**Q: Влияет ли перезагрузка индексов на работу бота?**  
A: Нет, перезагрузка происходит в фоне и не блокирует ответы пользователям.

**Q: Сколько времени занимает перезагрузка?**  
A: От нескольких секунд до 1-2 минут в зависимости от размера базы знаний.
