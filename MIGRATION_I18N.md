# Инструкция по миграции для системы интернационализации (i18n)

## Обзор изменений

В систему UQsoft была внедрена полноценная система интернационализации (i18n) с поддержкой 4 языков:
- 🇷🇺 Русский (ru)
- 🇰🇿 Казахский (kk)
- 🇬🇧 Английский (en)
- 🇨🇳 Китайский (zh)

## Шаг 1: Миграция базы данных

Если база данных уже существует, необходимо добавить поле `language` в таблицу `users`.

### Автоматическая миграция

Запустите скрипт миграции:

```bash
python -m app.utils.migrate_language
```

### Ручная миграция

Если автоматическая миграция не работает, выполните SQL-команду:

```sql
ALTER TABLE users ADD COLUMN language VARCHAR(2) NOT NULL DEFAULT 'ru';
```

## Шаг 2: Проверка структуры файлов

Убедитесь, что созданы следующие файлы и директории:

```
app/
├── core/
│   ├── locales/
│   │   ├── ru.json
│   │   ├── kk.json
│   │   ├── en.json
│   │   └── zh.json
│   └── i18n.py
├── bot/
│   ├── middlewares/
│   │   └── i18n.py
│   ├── handlers/
│   │   └── settings.py
│   └── keyboards/
│       └── language.py
└── utils/
    └── migrate_language.py
```

## Шаг 3: Запуск бота

После миграции запустите бота как обычно:

```bash
python main.py
```

## Архитектура системы i18n

### 1. I18nManager (`app/core/i18n.py`)

Класс-синглтон, который загружает JSON-файлы с переводами при старте.

**Использование:**

```python
from app.core.i18n import i18n

# Получение переведенной строки
text = i18n.get("welcome_text", lang="ru")

# Получение строки с параметрами
text = i18n.get("your_role", lang="en", role="Manager")
```

### 2. I18nMiddleware (`app/bot/middlewares/i18n.py`)

Middleware автоматически:
- Получает `user_id` из каждого апдейта
- Извлекает язык пользователя из БД
- Пробрасывает `lang` и `i18n` в `data` хендлера

**Регистрация в main.py:**

```python
from app.bot.middlewares.i18n import I18nMiddleware

dp.message.middleware(I18nMiddleware())
dp.callback_query.middleware(I18nMiddleware())
```

### 3. Обновленные хендлеры

Все хендлеры теперь принимают параметры:
- `lang: str` — код языка пользователя
- `i18n: I18nManager` — экземпляр менеджера локализации

**Пример:**

```python
@router.message(Command("start"))
async def cmd_start(
    message: Message,
    state: FSMContext,
    role: str | None = None,
    i18n: I18nManager | None = None,
    lang: str = "ru"
) -> None:
    welcome_text = i18n.get("welcome_text", lang)
    await message.answer(welcome_text)
```

### 4. Обновленные клавиатуры

Главное меню теперь принимает параметр `lang`:

```python
from app.bot.keyboards.main_menu import get_main_menu

keyboard = get_main_menu(role="employee", is_admin=False, lang="kk")
```

## Работа с локализацией

### Добавление новых строк

1. Откройте JSON-файл для каждого языка в `app/core/locales/`
2. Добавьте новый ключ и перевод
3. Используйте в коде через `i18n.get()`

**Пример добавления новой строки:**

```json
{
  "new_feature_title": "Новая функция",
  "new_feature_description": "Описание новой функции: {parameter}"
}
```

**Использование:**

```python
text = i18n.get("new_feature_description", lang, parameter="Test")
```

### Форматирование строк

Используйте `{parameter}` для подстановки значений:

```json
{
  "your_role": "Твоя роль: {role}"
}
```

```python
text = i18n.get("your_role", "ru", role="Менеджер")
# Результат: "Твоя роль: Менеджер"
```

## Настройки языка

### При регистрации

При первом запуске `/start` пользователь видит выбор языка из 4 вариантов.

### Смена языка

1. Пользователь нажимает на кнопку **"⚙️ Настройки"** в главном меню
2. Выбирает **"🌐 Сменить язык"**
3. Выбирает новый язык
4. Главное меню автоматически обновляется с новыми переводами

## Тестирование

Проверьте следующие сценарии:

1. **Новый пользователь:**
   - Выбор языка при первом запуске
   - Ввод инвайт-кода на выбранном языке
   - Выбор отдела на выбранном языке

2. **Существующий пользователь:**
   - Главное меню отображается на сохраненном языке
   - Кнопка "Настройки" работает
   - Смена языка обновляет все кнопки меню

3. **Функциональность:**
   - "🔍 Спроси базу" работает на всех языках
   - Кнопка "Назад в меню" переведена корректно
   - Все сообщения об ошибках переведены

## Поддерживаемые языки

| Код | Язык | Эмодзи флага |
|-----|------|--------------|
| ru  | Русский | 🇷🇺 |
| kk  | Қазақша | 🇰🇿 |
| en  | English | 🇬🇧 |
| zh  | 中文 | 🇨🇳 |

## Устранение неполадок

### Ошибка "Locales directory not found"

Проверьте, что директория `app/core/locales/` создана и содержит все 4 JSON-файла.

### Ошибка "Column 'language' not found"

Запустите миграцию:

```bash
python -m app.utils.migrate_language
```

### Кнопки не обновляются после смены языка

Перезапустите бота и проверьте логи. Убедитесь, что `I18nMiddleware` зарегистрирован в `main.py`.

## Заключение

Система i18n полностью интегрирована в бот. Все новые хендлеры должны использовать `i18n.get()` для получения переведенных строк вместо захардкоженного текста.

**Важно:** Никогда не используйте AI для перевода интерфейса. Все переводы хранятся в JSON-файлах.
