# Реализация удаления администраторов

## Обзор

Добавлена возможность удаления администраторов через админ-панель с защитой главного админа и самоудаления.

## Реализованные функции

### 1. Сервис удаления администратора

**Файл:** `app/services/admin_service.py`

```python
async def remove_admin(session: AsyncSession, user_id: int) -> bool:
    """
    Удаляет администратора из базы данных.
    
    Args:
        session: Сессия базы данных
        user_id: Telegram ID пользователя
    
    Returns:
        True если админ успешно удален, False если произошла ошибка
    """
```

**Функционал:**
- Поиск администратора в БД
- Удаление записи из таблицы `admins`
- Автоматический `commit` в БД
- Логирование всех операций

### 2. Обновленный список администраторов

**Файл:** `app/bot/handlers/admin.py`, функция `handle_admins_button`

**Изменения:**
- Каждый администратор отображается как inline-кнопка
- Рядом с админом (кроме защищенных) появляется кнопка удаления ❌
- Главный админ (ID: 375693711) помечен как 👑
- Текущий пользователь помечен как "(Вы)"

**Формат отображения:**
```
👥 Управление администраторами

Всего админов: 3

Выберите администратора для управления:

[👤 John Smith 👑]
[👤 Admin2 (Вы)]
[👤 Admin3] [❌]

[➕ Добавить админа]
```

### 3. Просмотр информации об администраторе

**Callback:** `admin_info:{user_id}`

**Функционал:**
- Показывает детальную информацию об админе
- Отображает ID и имя
- Показывает статус защиты (главный админ или текущий пользователь)
- Кнопка удаления (если админ не защищен)

**Пример:**
```
👤 Информация об администраторе

ID: 123456789
Имя: Admin3

[❌ Удалить администратора]
[◀️ Назад к списку]
```

### 4. Удаление администратора

**Callback:** `admin_remove:{user_id}`

**Процесс удаления:**

1. **Проверка безопасности:**
   - Проверка что удаляемый админ не является главным (ID: 375693711)
   - Проверка что админ не удаляет сам себя
   
2. **Удаление из БД:**
   - Вызов `remove_admin(session, user_id)`
   - Автоматический commit транзакции

3. **Уведомления:**
   - Подтверждение админу: "✅ Права администратора успешно отозваны!"
   - Уведомление пользователю: "⚠️ Ваши права администратора были отозваны"

4. **Результат:**
   ```
   ✅ Права администратора успешно отозваны!
   
   👤 Пользователь: Admin3 (ID: 123456789)
   
   📊 Осталось администраторов: 2
   
   [◀️ Назад к списку]
   ```

### 5. Возврат к списку

**Callback:** `admin_list`

Возвращает пользователя к списку администраторов после любой операции (просмотр, удаление).

## Защита от ошибок

### Защищенные администраторы

1. **Главный администратор (ID: 375693711)**
   - Помечен значком 👑
   - Не имеет кнопки удаления
   - При попытке удаления через callback: "❌ Главного администратора нельзя удалить!"

2. **Текущий пользователь**
   - Помечен как "(Вы)"
   - Не имеет кнопки удаления
   - При попытке самоудаления: "❌ Вы не можете удалить сами себя!"

### Логирование

Все операции логируются с префиксом `[ADMIN]`:

```python
logger.info(f"[ADMIN] Removed admin user_id={user_id}, username={username}")
logger.info(f"Admin {callback.from_user.id} removed admin rights from user {admin_user_id}")
logger.warning(f"[ADMIN] Admin with user_id={user_id} not found")
```

## Пользовательский сценарий

### Сценарий 1: Удаление администратора

1. Админ открывает "👥 Админы"
2. Видит список всех администраторов
3. Нажимает кнопку ❌ рядом с нужным админом
4. Система:
   - Проверяет права доступа
   - Удаляет запись из БД
   - Отправляет уведомление пользователю
   - Показывает подтверждение

### Сценарий 2: Просмотр информации

1. Админ открывает "👥 Админы"
2. Нажимает на кнопку с именем админа
3. Видит детальную информацию:
   - ID пользователя
   - Имя пользователя
   - Статус защиты
   - Кнопку удаления (если доступна)

### Сценарий 3: Попытка удалить главного админа

1. Админ открывает "👥 Админы"
2. Видит главного админа с иконкой 👑
3. У главного админа нет кнопки удаления
4. При попытке удалить через callback: предупреждение

### Сценарий 4: Попытка самоудаления

1. Админ открывает "👥 Админы"
2. Видит свой аккаунт помеченный как "(Вы)"
3. У своего аккаунта нет кнопки удаления
4. При попытке удалить через callback: предупреждение

## Технические детали

### База данных

**Таблица:** `admins`

**Структура:**
```python
class Admin(Base):
    __tablename__ = "admins"
    
    user_id: Mapped[int] = mapped_column(BigInteger, primary_key=True)
    username: Mapped[str] = mapped_column(String(255), nullable=False)
```

**Операции:**
- `INSERT` - добавление админа (`add_admin`)
- `SELECT` - проверка/получение админов (`is_admin`, `get_all_admins`)
- `DELETE` - удаление админа (`remove_admin`)

### Обработчики callback

| Callback | Описание |
|----------|----------|
| `admin_info:{user_id}` | Показывает информацию об админе |
| `admin_remove:{user_id}` | Удаляет админа |
| `admin_list` | Возвращает к списку админов |
| `admin_add_new` | Начинает процесс добавления нового админа |

## Безопасность

### Многоуровневая защита

1. **Проверка на уровне UI:**
   - Защищенные админы не имеют кнопки удаления
   
2. **Проверка на уровне callback:**
   - Проверка ID главного админа
   - Проверка самоудаления
   - Проверка прав доступа
   
3. **Проверка на уровне БД:**
   - Транзакции с rollback при ошибках
   - Логирование всех операций

### Константы защиты

```python
MAIN_ADMIN_ID = 375693711  # ID главного администратора (защищен от удаления)
```

## Тестирование

### Чек-лист функционала

- ✅ Список админов отображается корректно
- ✅ Главный админ помечен 👑 и не имеет кнопки удаления
- ✅ Текущий пользователь помечен "(Вы)" и не имеет кнопки удаления
- ✅ Обычные админы имеют кнопку удаления ❌
- ✅ Клик на имя админа открывает детальную информацию
- ✅ Удаление админа работает корректно
- ✅ Уведомление отправляется удаленному админу
- ✅ Подтверждение показывается текущему админу
- ✅ Возврат к списку работает корректно
- ✅ Защита от удаления главного админа работает
- ✅ Защита от самоудаления работает
- ✅ Логирование всех операций работает

### Тестовые сценарии

**Тест 1: Удаление обычного админа**
```
Действие: Админ удаляет другого админа
Ожидаемый результат: Админ удален, уведомление отправлено
```

**Тест 2: Попытка удалить главного админа**
```
Действие: Попытка удалить админа с ID 375693711
Ожидаемый результат: Ошибка "Главного администратора нельзя удалить!"
```

**Тест 3: Попытка самоудаления**
```
Действие: Админ пытается удалить сам себя
Ожидаемый результат: Ошибка "Вы не можете удалить сами себя!"
```

**Тест 4: Просмотр информации**
```
Действие: Клик на имя админа
Ожидаемый результат: Открывается карточка с детальной информацией
```

## Миграция

Изменения в БД не требуются - используется существующая таблица `admins`.

## Rollback

В случае необходимости отката изменений:

1. Откатить коммит в git
2. Перезапустить бота
3. Никаких миграций БД не требуется

## FAQ

**Q: Что произойдет если удалить всех админов кроме главного?**
A: Останется только главный админ, который всегда защищен от удаления.

**Q: Можно ли восстановить удаленного админа?**
A: Да, через кнопку "➕ Добавить админа" в меню управления администраторами.

**Q: Что произойдет с правами удаленного админа?**
A: Пользователь теряет доступ к админ-панели, но остается обычным сотрудником в системе.

**Q: Получит ли пользователь уведомление об удалении?**
A: Да, система автоматически отправит уведомление: "⚠️ Ваши права администратора были отозваны".

**Q: Можно ли удалить главного админа?**
A: Нет, главный админ (ID: 375693711) защищен от удаления на всех уровнях системы.
