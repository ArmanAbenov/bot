═══════════════════════════════════════════════════════════════
🚀 ФИНАЛЬНЫЕ КОМАНДЫ ДЛЯ ЗАГРУЗКИ НА GITHUB
═══════════════════════════════════════════════════════════════

⚠️  ПЕРЕД ВЫПОЛНЕНИЕМ:
1. Прочитайте GITHUB_DEPLOYMENT.md (особенно раздел "Безопасность")
2. Убедитесь, что .env файл НЕ показывается в git status

═══════════════════════════════════════════════════════════════
📋 КОМАНДЫ (копируйте и выполняйте последовательно в PowerShell)
═══════════════════════════════════════════════════════════════

# 1️⃣ Проверьте, что .env игнорируется
git status

# ❗ CRITICAL: Если видите .env в списке - ОСТАНОВИТЕСЬ!
#            Не выполняйте следующие команды!

# 2️⃣ Добавьте все файлы (кроме игнорируемых)
git add .

# 3️⃣ Создайте коммит
git commit -m "Initial commit: UQ-bot RAG system with multitenancy

- Telegram bot with aiogram 3.x
- RAG system with Google Gemini 2.5 Flash  
- FAISS vector store for knowledge base
- Multi-language support (ru, en, kk, zh)
- Department-based document isolation
- Hierarchical admin panel for KB management
- Voice message support (STT)
- Automatic index updates on file deletion"

# 4️⃣ Привяжите удаленный репозиторий GitHub
git remote add origin https://github.com/ArmanAbenov/bot.git

# 5️⃣ Проверьте remote
git remote -v

# 6️⃣ Отправьте код на GitHub
git push -u origin main

═══════════════════════════════════════════════════════════════
🎉 ГОТОВО! После успешного push:
═══════════════════════════════════════════════════════════════

1. Откройте https://github.com/ArmanAbenov/bot
2. Проверьте, что README.md отображается
3. Убедитесь, что .env файла НЕТ в репозитории
4. Добавьте описание и topics в настройках репозитория

═══════════════════════════════════════════════════════════════
❌ ЕСЛИ ВОЗНИКЛИ ПРОБЛЕМЫ:
═══════════════════════════════════════════════════════════════

❌ "remote origin already exists"
→ git remote remove origin
→ git remote add origin https://github.com/ArmanAbenov/bot.git

❌ "refused to merge unrelated histories"  
→ git pull origin main --allow-unrelated-histories
→ git push -u origin main

❌ ".env файл попал в коммит"
→ git rm --cached .env
→ git commit -m "Remove .env from tracking"
→ git push

❌ "failed to push some refs"
→ git pull origin main --rebase
→ git push -u origin main

═══════════════════════════════════════════════════════════════
📝 КРАТКАЯ ВЕРСИЯ (одной строкой):
═══════════════════════════════════════════════════════════════

git add . && git commit -m "Initial commit: UQ-bot RAG system" && git remote add origin https://github.com/ArmanAbenov/bot.git && git push -u origin main

⚠️  НО РЕКОМЕНДУЕТСЯ выполнять команды ПООЧЕРЕДНО для контроля!

═══════════════════════════════════════════════════════════════
